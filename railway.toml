[build]
builder = "nixpacks"

[variables]
HF_HUB_ENABLE_HF_TRANSFER = "1"
SENTENCE_TRANSFORMERS_HOME = "/tmp/.cache"

[deploy]
rootDirectory = "/"
startCommand = "uvicorn BACKEND.app.api:app --host 0.0.0.0 --port $PORT"